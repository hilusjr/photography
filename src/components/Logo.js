function Logo({ isScrolled }) {
  return (
    <>
      <div className="logo" data-scrolled={isScrolled}>
        <span>hilusjr</span>
        <span>photography</span>
      </div>
    </>
  )
}

export default Logo
